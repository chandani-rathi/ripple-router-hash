export component Page({path, params }) {
	<div class="flex-1 flex flex-col items-center justify-center">
		<h1 class="text-3xl font-semibold">{"Home"}</h1>
		<h2>{JSON.stringify({path, params})}</h2>
	</div>
}