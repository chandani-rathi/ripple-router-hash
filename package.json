{
	"name": "ripple-router-hash",
	"version": "0.0.1",
	"description": "",
	"type": "module",
	"scripts": {
		"watch:lib:hash": "npm run watch -w ripple-router-hash",
		"watch:apps:hash": "npm run dev -w simple-hash",
		"dev": "npx npm-run-all2 --parallel watch:**",
		"test": "vitest run"
	},
	"license": "MIT",
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"devDependencies": {
		"jsdom": "^27.0.0",
		"vite-plugin-ripple": "^0.2.41",
		"vitest": "^3.2.4"
	}
}
